% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clu_utils.R
\name{cluutils}
\alias{cluutils}
\title{Clustering utilities}
\usage{
cluutils()
}
\value{
returns a \code{cluutils} object exposing metric and selector helpers.
}
\description{
Utility object that groups clustering metrics and model-selection helpers.
}
\details{
The object organizes helpers into two semantic groups:

\strong{Metrics}
\itemize{
\item \code{metric_wcss()} computes the total within-cluster sum of squares.
\item \code{metric_silhouette()} computes the mean silhouette score from pairwise distances.
\item \code{metric_entropy()} computes external clustering entropy against a reference label.
\item \code{metric_purity()} computes cluster purity against a reference label.
\item \code{metric_davies_bouldin()} computes the Davies-Bouldin index.
\item \code{metric_calinski_harabasz()} computes the Calinski-Harabasz score.
\item \code{metric_adjusted_rand_index()} computes the adjusted Rand index.
\item \code{metric_noise_points()} summarizes the number of noise points in density-based clustering.
\item \code{metric_loglik()} and \code{metric_modularity()} expose model-specific quality summaries.
}

\strong{Selectors}
\itemize{
\item \code{selector_best()} selects the best hyperparameter value by direct optimization.
\item \code{selector_elbow()} selects the elbow of a metric curve via maximum curvature.
}

Metric helpers return a standardized list with fields \code{metric}, \code{value}, \code{goal},
and \code{type}. This keeps the contract uniform even when the metrics themselves differ.
}
\examples{
utils <- cluutils()

data(iris)
x <- iris[, 1:4]
clu <- stats::kmeans(x, centers = 3)$cluster

utils$metric_wcss(x, clu)
utils$metric_silhouette(x, clu)
utils$metric_entropy(clu, iris$Species)
utils$selector_best(c(0.31, 0.42, 0.39), goal = "maximize")
}
