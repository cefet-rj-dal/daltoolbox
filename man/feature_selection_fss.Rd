% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_feature_selection_fss.R
\name{feature_selection_fss}
\alias{feature_selection_fss}
\title{Feature selection by forward stepwise search}
\usage{
feature_selection_fss(attribute, features = NULL)
}
\arguments{
\item{attribute}{target attribute name}

\item{features}{optional vector of feature names (default: all columns except \code{attribute})}
}
\value{
returns an object of class \code{feature_selection_fss}
}
\description{
Selects numeric predictors using forward stepwise subset search.
}
\details{
Uses \code{leaps::regsubsets} and keeps the subset with the highest adjusted R-squared.
The target attribute must be numeric.
}
\examples{
if (requireNamespace("leaps", quietly = TRUE)) {
 data(Boston)
 fs <- feature_selection_fss("medv")
 fs <- fit(fs, Boston)
 fs$selected
 boston_fs <- transform(fs, Boston)
 names(boston_fs)
}
}
