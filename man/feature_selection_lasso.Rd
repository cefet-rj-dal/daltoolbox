% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_feature_selection_lasso.R
\name{feature_selection_lasso}
\alias{feature_selection_lasso}
\title{Feature selection by lasso}
\usage{
feature_selection_lasso(attribute, features = NULL)
}
\arguments{
\item{attribute}{target attribute name}

\item{features}{optional vector of feature names (default: all numeric columns except \code{attribute})}
}
\value{
returns an object of class \code{feature_selection_lasso}
}
\description{
Selects predictors using L1-regularized regression.
}
\details{
Fits a lasso path with \code{glmnet} and keeps predictors with non-zero coefficients at \code{lambda.min}.
The target attribute must be numeric.
}
\examples{
if (requireNamespace("glmnet", quietly = TRUE)) {
 data(Boston)
 fs <- feature_selection_lasso("medv")
 fs <- fit(fs, Boston)
 fs$selected
 boston_fs <- transform(fs, Boston)
 names(boston_fs)
}
}
