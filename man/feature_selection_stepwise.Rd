% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_feature_selection_stepwise.R
\name{feature_selection_stepwise}
\alias{feature_selection_stepwise}
\title{Feature selection by stepwise model selection}
\usage{
feature_selection_stepwise(
  attribute,
  features = NULL,
  direction = "forward",
  family = stats::binomial,
  trace = 0
)
}
\arguments{
\item{attribute}{target attribute name}

\item{features}{optional vector of feature names (default: all columns except \code{attribute})}

\item{direction}{stepwise direction: "forward", "backward", or "both"}

\item{family}{glm family passed to \code{stats::glm} (default: \code{binomial})}

\item{trace}{level of tracing from \code{stats::step}}
}
\value{
returns an object of class \code{feature_selection_stepwise}
}
\description{
Select features using stepwise search over generalized linear models.
}
\details{
Supports forward, backward, and both directions via \code{stats::step}.
}
\examples{
data(iris)
fg <- feature_generation(
 IsVersicolor = ifelse(Species == "versicolor", "versicolor", "not_versicolor")
)
iris_bin <- transform(fg, iris)
iris_bin$IsVersicolor <- factor(iris_bin$IsVersicolor)
fs <- feature_selection_stepwise("IsVersicolor", direction = "forward")
fs <- fit(fs, iris_bin)
fs$selected
transform(fs, iris_bin) |> names()
}
