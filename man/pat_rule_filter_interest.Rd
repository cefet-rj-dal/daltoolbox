% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pat_rule_filter.R
\name{pat_rule_filter_interest}
\alias{pat_rule_filter_interest}
\title{Interesting rule filter}
\usage{
pat_rule_filter_interest(
  lift_min = 1,
  kulc_min = NULL,
  ir_max = NULL,
  support_min = NULL,
  confidence_min = NULL,
  count_min = NULL
)
}
\arguments{
\item{lift_min}{Minimum lift. Use \code{NULL} to ignore lift.}

\item{kulc_min}{Minimum Kulczynski measure. Use \code{NULL} to ignore Kulczynski.}

\item{ir_max}{Maximum imbalance ratio. Use \code{NULL} to ignore imbalance ratio.}

\item{support_min}{Minimum support. Use \code{NULL} to ignore support.}

\item{confidence_min}{Minimum confidence. Use \code{NULL} to ignore confidence.}

\item{count_min}{Minimum rule count. Use \code{NULL} to ignore count.}
}
\value{
A \code{pat_rule_filter_interest} object.
}
\description{
Build a rule filter based on classical interest measures.
}
\details{
This filter follows the usual post-processing step for association rules:
rules are discovered first, then kept only when their interest measures pass
user-defined thresholds. \code{lift > 1}, high Kulczynski, and low imbalance ratio
are common criteria discussed in association-rule mining texts such as Han,
Kamber, and Pei.
}
\examples{
filter <- pat_rule_filter_interest(lift_min = 1, kulc_min = 0.6, ir_max = 0.9)
class(filter)
}
