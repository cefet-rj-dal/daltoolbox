% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_imputation_predictive.R
\name{imputation_predictive}
\alias{imputation_predictive}
\title{Predictive imputation base}
\usage{
imputation_predictive(target, sources = NULL, method = c("median", "mean"))
}
\arguments{
\item{target}{target column to impute}

\item{sources}{optional vector of predictor column names}

\item{method}{initial imputation method for numeric source columns: "median" or "mean"}
}
\value{
returns an object of class \code{imputation_predictive}
}
\description{
Base class for supervised imputers that learn one target column from a set of source columns.
}
\details{
The target column is the attribute to be imputed. The source columns are the predictors used
to estimate missing target values. If \code{sources = NULL}, all supported columns except the target are used.
Missing values in source columns can be pre-imputed by a simpler method before fitting the predictive model.
}
\examples{
data(iris)
imp <- imputation_predictive(
 "Sepal.Length",
 sources = c("Sepal.Width", "Petal.Length", "Petal.Width", "Species")
)
class(imp)
}
