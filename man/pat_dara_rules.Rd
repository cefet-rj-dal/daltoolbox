% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pat_dara.R
\name{pat_dara_rules}
\alias{pat_dara_rules}
\title{Generate target association rules for DARA}
\usage{
pat_dara_rules(
  data,
  rhs,
  supp = 0,
  conf = 0,
  support_strategy = pat_support_threshold("curvature"),
  confidence_strategy = pat_confidence_threshold(),
  minlen = 3,
  maxlen = 4,
  quality_filter = NULL,
  rule_filter = pat_rule_filter_none(),
  remove_redundant = TRUE,
  control = list(verbose = FALSE)
)
}
\arguments{
\item{data}{A data frame or \code{transactions} object.}

\item{rhs}{Right-hand-side item, such as \code{"income=small"}.}

\item{supp}{Minimum support. If \code{0}, estimated during \code{fit()} using \code{support_strategy}.}

\item{conf}{Minimum confidence. If \code{0}, estimated during \code{fit()} using \code{confidence_strategy}.}

\item{support_strategy}{support threshold strategy created with \code{pat_support_threshold()}}

\item{confidence_strategy}{confidence threshold strategy created with \code{pat_confidence_threshold()}}

\item{minlen}{Minimum rule length.}

\item{maxlen}{Maximum rule length.}

\item{quality_filter}{Optional quality filter created with \code{patutils()}.}

\item{rule_filter}{rule filter created with \code{pat_rule_filter_none()}, \code{pat_rule_filter_interest()}, or \code{pat_rule_filter_dara()}.}

\item{remove_redundant}{If \code{TRUE}, remove redundant rules using \code{arules::is.redundant}.}

\item{control}{Apriori control list.}
}
\value{
A tidy rule data frame.
}
\description{
Mine association rules for a fixed right-hand side and return them as a data frame.
}
\examples{
if (requireNamespace("arules", quietly = TRUE)) {
  data("AdultUCI", package = "arules")
  data <- as.data.frame(AdultUCI)
  data <- data[, c("workclass", "education", "marital-status", "occupation", "income")]
  rules <- pat_dara_rules(data, rhs = "income=small", supp = 0.05, conf = 0.6)
  head(rules)
}
}
