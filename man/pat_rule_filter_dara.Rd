% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pat_rule_filter.R
\name{pat_rule_filter_dara}
\alias{pat_rule_filter_dara}
\title{DARA rule filter}
\usage{
pat_rule_filter_dara(
  data,
  rhs_attr,
  rhs_value = NULL,
  attributes = NULL,
  min_score = 1,
  top = NULL,
  score = c("sum", "mean", "max"),
  use_abs = FALSE,
  rule_weight = NULL
)
}
\arguments{
\item{data}{Reference data frame used by DARA.}

\item{rhs_attr}{Right-hand-side attribute used by the mined rules.}

\item{rhs_value}{Optional right-hand-side value used to restrict \code{data}.}

\item{attributes}{Optional attributes to compare. Defaults to common columns.}

\item{min_score}{Minimum DARA score required to keep a rule.}

\item{top}{Optional number of highest-scored rules to keep.}

\item{score}{Rule score aggregation: \code{"sum"}, \code{"mean"}, or \code{"max"}.}

\item{use_abs}{If \code{TRUE}, rank by absolute divergence counters.}

\item{rule_weight}{Optional numeric rule column used to weight rule frequencies.}
}
\value{
A \code{pat_rule_filter_dara} object.
}
\description{
Build a rule filter based on Divergent Association Ranking Analysis.
}
\details{
DARA filters rules by scoring each antecedent with the divergence counters
computed by \code{pat_dara()}. A rule with antecedent values that are more prominent
in the rule set than in the exploratory data receives a higher positive score.
}
\examples{
data <- data.frame(a = factor(c("x", "x", "y")), b = factor(c("z", "w", "z")))
filter <- pat_rule_filter_dara(data, rhs_attr = "b", rhs_value = "z")
class(filter)
}
