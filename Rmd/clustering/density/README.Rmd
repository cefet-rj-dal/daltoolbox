# Density-Based Clustering Examples

This block separates methods that detect dense regions from those that force every instance into a centroid or medoid partition.

- [10-density-dbscan.md](/examples/clustering/density/10-density-dbscan.md) - `clu_dbscan`: groups dense neighborhoods and can leave noise points unassigned.
