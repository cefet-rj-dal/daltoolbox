# Dataset Summaries Examples

These examples restructure the dataset at the group level instead of only modifying single rows or columns.

> Slide deck: none yet. `d031-data-preprocessing.pdf` describes preprocessing as "8 steps" and never counted aggregation among them, so this topic still needs a new deck (e.g. `d040-aggregation.pdf`).

- [80-aggregation-by-group.md](/examples/transf/aggregation/80-aggregation-by-group.md) - `aggregation`: grouped summaries through named expressions.
